int x;
int y;
void c;
bool d;
struct s {
    int x;
    int y;
    void c;
    bool d;
};
void func() {
    int x;
    bool y;
    void c;
    bool d;
    int l;
    struct s d;
    struct d s;
    y = x;
    x = y = d;
    x = (x + x);
    x = (x - x);
    x = (x * x);
    x = (x / x);
    x = (!x);
    x = (x && y);
    x = (x || y);
    x = (x == y);
    x = (x != y);
    x = (x < y);
    x = (x > y);
    x = (x <= y);
    x = (x >= y);
    x = (-y);
    y = ((((-x) * x) - x) + (x / x));
    x++;    x--;    x = "test";
    return;
    return a;
}
void Program(int a, int b) {
    cin >> a;
    cout << b;
}
int main() {
    c = TRUE;
    k = Program(a, b);
    if ((b == (-1))) {
        x = ((4 + (3 * 5)) - y);
        while (c) {
            y = ((y * 2) + x);
        }
    }
    else {
        x = 0;
    }
}
